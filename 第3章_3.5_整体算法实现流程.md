# 第 3 章 3.5 整体算法实现流程

本节结合 `fl_system.py` 中 `FederatedLearningSystem.run` 的实际控制流，梳理**完整通信轮次**，并给出与经典 **FedAvg 算法框图**同构的伪代码排版：先列出**输入 / 输出**，再给出**服务端主过程**，子程序 **ClientUpdate** 对应本地训练；服务端在聚合阶段不再使用 FedAvg 的样本量加权平均，而依次执行 **元梯度优化**（更新 \(\boldsymbol{z}\)）与 **带 \(\lambda\) 平滑及 \(s\) 收缩的参数写回**（更新 \(\boldsymbol{\theta}\)）。记号与第 3.1–3.4 节严格对齐：全局 \(\boldsymbol{\theta}\)，客户端上传 \(\boldsymbol{\theta}^{k}\)（本轮），\(\boldsymbol{z}\)、\(\boldsymbol{u}=\mathrm{softmax}(\boldsymbol{z})\)，代理元损失 \(\mathcal{L}_p\)，\(\lambda\)、\(s\)、\(\tilde{w}_k\)、\(\beta_k\)。

**与标准 FedAvg 图的差异（实现事实）**：`fl_system.py` **每轮选取全部 \(K\) 个客户端**参与上传，不设「每轮随机子集 \(S_t\)」及聚合比例 \(C\)；若论文需与经典 FedAvg 图示完全同形，可将下文集合 \(\mathcal{S}_t\) 理解为 \(\mathcal{S}_t=\{1,\ldots,K\}\)。

---

## 3.5.1 通信轮次与源码对应关系（文字）

**初始化（`run` 中 `for r in range(global_rounds)` 之前）**  
`set_seed` → `prepare_data` 得到训练集、测试集、\(\mathcal{D}_p\)、\(\{\mathcal{C}_k\}\) → 构造 `test_loader` → `create_model` 初始化全局 \(\boldsymbol{\theta}\) → 构造 `ServerLearnableAggregator`（含 \(\boldsymbol{z}\) 与优化器 \(\mathcal{O}_z\)，学习率即 `aggregation_weight_lr`）。

**第 \(t\) 轮（`r = t`，共 \(T=\) `global_rounds` 轮）**

1. **本地训练**：对每个 \(k\in\{1,\ldots,K\}\)，以当前全局 \(\boldsymbol{\theta}\) 为起点，在 \(\mathcal{C}_k\) 上执行 `local_epochs` 轮本地 SGD（含动量），得到 \(\boldsymbol{\theta}^{k}\)（实现：`ClientAdaptiveWeighted.run_local_training`）。  
2. **元梯度优化**：`aggregate_round` 内对 \(\mathcal{D}_p\) 各 batch 构造 \(\boldsymbol{u}=\mathrm{softmax}(\boldsymbol{z})\) 与混合参数 \(\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\)（含 \(s\) 与本轮起始 \(\boldsymbol{\theta}\) 的锚点前向，见 3.3–3.4），累计 \(\mathcal{L}_p\)，`backward` 后 \(\mathcal{O}_z.\texttt{step}()\) 更新 \(\boldsymbol{z}\)。  
3. **带平滑与收缩的聚合**：`no_grad` 下计算 \(\tilde{w}_k\)、\(\beta_k=s\tilde{w}_k\)，写回 \(\boldsymbol{\theta}\leftarrow\sum_k\beta_k\boldsymbol{\theta}^{k}+(1-s)\boldsymbol{\theta}\)（右端旧 \(\boldsymbol{\theta}\) 为写回前快照）。  
4. **评测与日志**：在 \(\mathcal{D}_{\mathrm{test}}\) 上算 Top-1 准确率；记录 \(\boldsymbol{\beta}\)（实现中写入 `_run_log.npz`）。  

**终止后**：保存 \(\boldsymbol{\theta}\) 的 `state_dict`（`.pth`）及可选运行日志（`.npz`）。

---

## 3.5.2 算法伪代码（与 FedAvg 图式对齐）

下列伪代码采用与教材 FedAvg 相同的**行编号风格**：**服务端执行**为主过程，**ClientUpdate** 为子程序；额外增加 **ServerMetaAndAggregate** 封装第 2–3 步，以严格对应「元梯度优化 → 带平滑与收缩的参数聚合」。

---

**算法 3-1** 基于代理元梯度及 \((\lambda,s)\) 稳健聚合的联邦训练（对应 `FederatedLearningSystem` + `aggregate_round`）

---

**输入：**  
\(K\)：客户端总数（`num_clients`）；  
\(T\)：通信轮数（`global_rounds`）；  
\(E\)：每客户端本地 epoch 数（`local_epochs`）；  
\(B\)：本地 mini-batch 大小（`batch_size`）；  
\(\eta\)：客户端学习率（`learning_rate`）；  
\(\eta_z\)：服务端 \(\boldsymbol{z}\) 学习率（`aggregation_weight_lr`）；  
动量：客户端与 \(\mathcal{O}_z\) 各取自配置（`momentum`）；  
\(\mathcal{D}_p\)：代理集；代理 mini-batch 大小；  
\(\lambda\in[0,1)\)：权平滑系数（`lam`）；  
\(s\in(0,1]\)：收缩系数（`shrink_sum`）；  
\(\{\mathcal{C}_k\}_{k=1}^K\)：各客户端训练索引；随机种子等（`seed`）。

**输出：**  
终局全局参数 \(\boldsymbol{\theta}\)（及实现中的每轮测试精度、\(\boldsymbol{\beta}\) 轨迹）。

---

**【服务端执行】**

| 行号 | 语句 |
|:----:|------|
| 1 | **初始化** 全局模型参数 \(\boldsymbol{\theta}\)；初始化聚合 logit \(\boldsymbol{z}\in\mathbb{R}^K\)（如 \(\boldsymbol{z}\leftarrow\mathbf{0}\)）；初始化仅作用于 \(\boldsymbol{z}\) 的优化器 \(\mathcal{O}_z\)（SGD，步长 \(\eta_z\)，含动量）。 |
| 2 | **对** 通信轮次 \(t=0,1,\ldots,T-1\) **执行** |
| 3 | \(\quad\) 令 \(\mathcal{S}_t\leftarrow\{1,2,\ldots,K\}\)（本实现每轮全客户端参与，与 `fl_system.py` 一致）。 |
| 4 | \(\quad\) **对** 每个客户端 \(k\in\mathcal{S}_t\) **（可并行）执行** |
| 5 | \(\quad\quad\) \(\boldsymbol{\theta}^{k}\leftarrow \textbf{ClientUpdate}\big(k,\boldsymbol{\theta}\big)\)。 |
| 6 | \(\quad\) **【元梯度优化 + 带平滑与收缩的参数聚合】** |
| 7 | \(\quad\) \(\boldsymbol{\theta},\boldsymbol{z}\leftarrow \textbf{ServerMetaAndAggregate}\big(\boldsymbol{\theta},\{\boldsymbol{\theta}^{k}\}_{k\in\mathcal{S}_t},\boldsymbol{z},\mathcal{D}_p,\lambda,s,\mathcal{O}_z\big)\)。 |
| 8 | \(\quad\) （可选）在 \(\mathcal{D}_{\mathrm{test}}\) 上评估 Top-1 精度并记录 \(\boldsymbol{\beta}=(\beta_1,\ldots,\beta_K)^\top\)（实现：`fl_system.py` 每轮评测与 `npz` 日志）。 |
| 9 | **结束** \(t\) |
| 10 | **返回** \(\boldsymbol{\theta}\)。 |

---

**【子程序】** \(\textbf{ClientUpdate}(k,\boldsymbol{\theta})\)

| 行号 | 语句 |
|:----:|------|
| 11 | \(\boldsymbol{w}\leftarrow\) 复制当前全局 \(\boldsymbol{\theta}\)。 |
| 12 | 将客户端 \(k\) 的私有索引集 \(\mathcal{C}_k\) 划分为若干大小为 \(B\) 的 mini-batch（实现：`DataLoader(Subset(...))`）。 |
| 13 | **对** \(e=1,\ldots,E\) **执行** |
| 14 | \(\quad\) **对** 每个 mini-batch \(b\) **执行** |
| 15 | \(\quad\quad\) \(\boldsymbol{w}\leftarrow \boldsymbol{w}-\eta\,\nabla_{\boldsymbol{w}}\ell(\boldsymbol{w};b)\)（实现中在 SGD 步上叠加动量）。 |
| 16 | \(\quad\) **结束** batch |
| 17 | **结束** \(e\) |
| 18 | **返回** \(\boldsymbol{w}\)（即该轮上传的 \(\boldsymbol{\theta}^{k}\)）。 |

---

**【子程序】** \(\textbf{ServerMetaAndAggregate}(\boldsymbol{\theta},\{\boldsymbol{\theta}^{k}\},\boldsymbol{z},\mathcal{D}_p,\lambda,s,\mathcal{O}_z)\)

| 行号 | 语句 |
|:----:|------|
| 19 | 记本轮写回前全局快照 \(\boldsymbol{\theta}^{-}\leftarrow\boldsymbol{\theta}\)（实现：使用 `state_dict` 中张量参与计算）。 |
| 20 | \(\mathcal{O}_z.\texttt{zero\_grad}()\)。 |
| 21 | **对** \(\mathcal{D}_p\) 的每个 mini-batch \(\mathcal{B}\) **执行** |
| 22 | \(\quad\) \(\boldsymbol{u}\leftarrow \mathrm{softmax}(\boldsymbol{z})\)。 |
| 23 | \(\quad\) 按 3.3–3.4 节构造混合参数 \(\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\)（系数含 \(s\) 与锚点 \(\boldsymbol{\theta}^{-}\)，对客户端 \(\boldsymbol{\theta}^{k}\) 使用 `detach` 后装配；前向用 `functional_call`）。 |
| 24 | \(\quad\) 累加 batch 交叉熵至 \(\mathcal{L}_p\)（实现：对 batch 均值再对 batch 数取平均）。 |
| 25 | **结束** \(\mathcal{B}\) |
| 26 | \(\mathcal{L}_p.\texttt{backward}()\)；\(\mathcal{O}_z.\texttt{step}()\)（**元梯度优化**：更新 \(\boldsymbol{z}\)）。 |
| 27 | **在** \(\texttt{no\_grad}\) **下执行（带 \(\lambda\) 平滑与 \(s\) 收缩的参数聚合）** |
| 28 | \(\quad\) \(\boldsymbol{u}\leftarrow \mathrm{softmax}(\boldsymbol{z})\)。 |
| 29 | \(\quad\) \(\tilde{w}_k\leftarrow \dfrac{(1-\lambda)\,u_k+\lambda/K}{\sum_{j=1}^{K}\big((1-\lambda)\,u_j+\lambda/K\big)}\)，\(k=1,\ldots,K\)。 |
| 30 | \(\quad\) \(\beta_k\leftarrow s\,\tilde{w}_k\)。 |
| 31 | \(\quad\) \(\boldsymbol{\theta}\leftarrow \displaystyle\sum_{k=1}^{K}\beta_k\,\boldsymbol{\theta}^{k}+(1-s)\,\boldsymbol{\theta}^{-}\)。 |
| 32 | **返回** \((\boldsymbol{\theta},\boldsymbol{z})\)。 |

---

**符号与 3.1–3.4 节对齐表**

| 符号 | 含义 |
|------|------|
| \(\boldsymbol{\theta}\) | 全局可学习参数（3.1、3.4） |
| \(\boldsymbol{\theta}^{k}\) | 第 \(t\) 轮客户端 \(k\) 上传（3.1；伪代码行 5） |
| \(\boldsymbol{z},\boldsymbol{u}\) | 聚合 logit 与 Softmax 输出（3.3；行 22、28） |
| \(\mathcal{L}_p\) | 代理集上的元损失（3.3；行 21–26） |
| \(\lambda\) | 均匀平滑系数（3.4；行 29） |
| \(s\) | 收缩系数（3.4；行 23、30） |
| \(\tilde{w}_k,\beta_k\) | 平滑后权与最终客户端系数（3.4；行 29–30） |

---

## 3.5.3 与经典 FedAvg 图式的一行对照

| FedAvg 标准图 | 本文算法 3-1 |
|----------------|----------------|
| 行 4–5 随机选子集 \(S_t\) | 行 3：\(\mathcal{S}_t=\{1,\ldots,K\}\)（全参与） |
| 行 6–8 `ClientUpdate` | 行 4–5、子程序 11–18 |
| 行 9 \(w_{t+1}\leftarrow\sum \frac{n_k}{n}w_{t+1}^k\) | 行 7、子程序 27–31：**元梯度**更新 \(\boldsymbol{z}\) 后，用 \(\beta_k\) 与锚点 \((1-s)\boldsymbol{\theta}^{-}\) 写回 \(\boldsymbol{\theta}\) |

---

## 3.5.4 本章方法作为自动化闭环系统

数据与索引在 3.2 节一次性确定后，**每一轮**由服务端广播同一 \(\boldsymbol{\theta}\)，各客户端独立执行 **ClientUpdate**（本地监督学习）；服务端在**同一轮**内自动完成 **ServerMetaAndAggregate**：先用 \(\mathcal{L}_p\) 驱动 \(\boldsymbol{z}\) 演化，再用 \((\lambda,s)\) 将凸组合与锚点写回 \(\boldsymbol{\theta}\)，形成「本地漂移 → 代理评价 → 权向量学习 → 稳健全局更新」的**因果链**。测试精度与 \(\boldsymbol{\beta}\) 轨迹提供**可观测反馈**，训练结束自动落盘。该流程完全由 `FederatedLearningSystem.run` 与 YAML 配置驱动，无需人工干预聚合系数，故本章方法在工程上构成**自动化闭环**。

---

*（源码：`fl_system.py` 第 29–122 行主循环及日志落盘；`sever.adaptive_weighting_advanced.adaptive_aggregate` → `ServerLearnableAggregator.aggregate_round`。）*
