# 4.5 病态分布对比实验

本节基于服务器实验产物目录 `experiments/outputs_server_autodl/outputs/` 中与 **病态（pathological）划分** 相关的汇总表与曲线图，给出实验设定、定量结果及简要讨论。与 **Dirichlet Non-IID** 主实验并列，用于体现任务书中「除狄利克雷外再考察一种经典强异构划分」的要求。

---

## 4.5.1 实验设定

- **数据集与模型：** CIFAR-10；全局与各客户端采用与第 4.3 节一致的轻量 CNN 结构及数据预处理（与 `configs/ch4_compare.yaml` 对齐，仅划分方式不同）。
- **病态划分含义：** 按实现 `dataset/data_utils.py` 中 `_split_pathological`，在打乱类别顺序后，将 **10 个类别尽量均分至 10 个客户端**，使 **每个客户端在本地训练阶段仅观测到其中一类样本**（类间不重叠），属于典型的 **标签倾斜 / 极端 Non-IID**。
- **客户端与通信：** 客户端数 \(K=10\)；每轮各客户端本地训练 **2** 个 epoch，批大小 **128**，SGD 学习率 **0.01**、动量 **0.9**；全局通信轮数 **30**。
- **对比方法：** FedAvg、FedProx（\(\mu=0.01\)）、本文自适应聚合（Adaptive）；服务端与代理相关超参与 `configs/ch4_pathological.yaml` 一致（如 `proxy_ratio=0.04`、`proxy_disjoint_from_clients=true`、`lam=0.1`、`aggregation_weight_lr=0.01`、`shrink_sum=1.0` 等）。
- **关于命令行 `--betas`：** 对比脚本对每个 `dirichlet_beta` 取值仍会改写配置字段并独立跑完全程；在 **`partition=pathological`** 时，**客户端索引划分不依赖 \(\beta\)**（见 `prepare_data` 注释），故表中 \(\beta\in\{0.1,0.5\}\) 两行在划分意义上为 **同一病态设定下的重复实验组**，仅用于与 Dirichlet 扫描接口保持一致；若论文版面紧张，可只保留一组或合并表述。

评价指标与第 4.2 节一致：各通信轮次在 **全局 IID 测试集** 上的 Top-1 准确率（%）、测试集样本平均交叉熵（曲线见 `comparison_loss_*_pathological.png`）；Adaptive 另记录代理集上的 meta-loss（`comparison_proxy_loss_*_pathological.png`）。

---

## 4.5.2 定量结果

表 4-X 给出病态划分下、**第 30 轮** 全局模型在测试集上的 **末轮准确率**，以及是否达到第 4.2 节预设阈值 \(T\in\{50\%,55\%,60\%\}\) 的 **首个通信轮次**（摘自 `ch4_comparison_summary.csv`；本目录下该文件为当次病态对比写出的汇总）。

**表 4-X　CIFAR-10 病态划分下三方法末轮测试精度与达阈值轮次（`global_rounds=30`）**

| \(\beta\)（配置字段，病态下不参与划分） | 方法 | 末轮测试精度（%） | 达 50% 的首轮 | 达 55% 的首轮 | 达 60% 的首轮 |
|----------------------------------------|------|-------------------|---------------|---------------|---------------|
| 0.1 | FedAvg | 10.00 | — | — | — |
| 0.1 | FedProx | 10.00 | — | — | — |
| 0.1 | Adaptive | 10.00 | — | — | — |
| 0.5 | FedAvg | 10.00 | — | — | — |
| 0.5 | FedProx | 10.00 | — | — | — |
| 0.5 | Adaptive | 10.00 | — | — | — |

说明：**10.00%** 与 CIFAR-10 **十类均匀随机猜测** 的期望精度同量级，表明在本文默认超参与 30 轮设定下，**三种方法均未学到可泛化到全类测试集的表示**；阈值列空表示 **30 轮内从未达到** 对应准确率。

---

## 4.5.3 曲线与可复核文件

同目录下保存有病态设定下的对比曲线（文件名含 `cifar10_pathological`），可与表 4-X 对照复核：

- 准确率：`comparison_acc_beta0p1_cifar10_pathological.png`、`comparison_acc_beta0p5_cifar10_pathological.png`
- 测试集平均交叉熵：`comparison_loss_beta0p1_cifar10_pathological.png`、`comparison_loss_beta0p5_cifar10_pathological.png`
- 代理 meta-loss（仅 Adaptive 有有效数值）：`comparison_proxy_loss_beta0p1_cifar10_pathological.png`、`comparison_proxy_loss_beta0p5_cifar10_pathological.png`
- 数值曲线：`comparison_acc_beta0p1_cifar10_pathological.npz`、`comparison_acc_beta0p5_cifar10_pathological.npz`（内含 `acc_*`、`test_loss_*`、`proxy_loss_*` 等字段）

---

## 4.5.4 讨论与写作建议

1. **与 Dirichlet 主实验对比：** Dirichlet 设定下（见 `ch4_comparison_summary_cifar10_dirichlet.csv` 或论文第 4.4 节表图），三方法可达 **远高于随机** 的测试精度；病态划分在本实现中 **极端到「每客户端仅一类」**，全局模型需在少量通信下融合 **互不重叠的类条件分布**，难度显著高于中等 \(\alpha\) 的 Dirichlet 场景。
2. **结果解读：** 不宜将「末轮 10%」简单归结为某一算法缺陷；更稳妥的表述是：在 **当前网络深度、学习率、局部 epoch 数与代理规模** 下，三种聚合策略 **均未能** 在该病态设定上取得有效泛化。若审稿人或答辩关注该点，可在局限中说明：**病态实验用于展示极端异构难度边界**，主结论仍以 Dirichlet 扫描为主；或补充 **调参、延长轮数、调整每客户端可见类数（如减少 \(K\) 或采用 shard 数小于类数）** 的附加实验作为展望。
3. **符号统一：** 正文若将狄利克雷参数记为 \(\alpha\)，可与第 4 章其它小节一致，并注明病态表中 \(\beta\) 仅为配置字段扫描值、**不改变**病态划分本身。

---

*本文档由仓库内 `experiments/outputs_server_autodl/outputs/ch4_comparison_summary.csv` 与同名曲线文件归纳生成，表号「4-X」请在定稿时按全论文章节统一编号。*
