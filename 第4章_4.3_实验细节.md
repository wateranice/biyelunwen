# 4.3 实验细节

**超参数总表（\(K,E,B,\eta,p,\lambda,s,\alpha\) 等）与逐项选择依据** 已单独整理为 **`第4章_4.3_实验参数配置.md`**（表 4-1，对应 `configs/default.yaml`），本节侧重 **软硬件环境、数据与模型、复现命令**，避免与参数表重复。

---

## 4.3.1 软硬件与运行环境

全部训练与对比实验在租用的 **云端 GPU 实例** 上完成。控制台给出的 **镜像与驱动** 为：**Ubuntu 22.04**，**Python 3.12**，**PyTorch 2.8.0**，**CUDA 12.8**；实例配备 **12 vCPU**（Intel Xeon Platinum 8260 @ 2.40GHz）、**62 GB** 内存、**1 块 vGPU（显存 32 GB）**；系统盘 **30 GB**、数据盘 **50 GB SSD**。长时间任务与代码、数据集、权重均置于数据盘目录（如 `/root/autodl-tmp/`），以避免占满系统盘。

**说明：** 若在实际调试中为兼容 NumPy / 显卡驱动而 **更换了 PyTorch 与 CUDA 的 wheel 组合**（例如使用 `cu118` 预编译包），论文中可补充一句「以运行当时 `python -c "import torch; print(torch.__version__, torch.version.cuda)"` 输出为准」，与租用面板上的默认镜像版本区分即可。

随机性方面，所有实验固定 **随机种子 `seed=42`**（见配置文件 `experiment.seed`），包括对 Dirichlet 划分、代理集抽样等与随机数相关的步骤，以保证 FedAvg、FedProx 与本文方法在 **同一数据划分与同一初始化** 下公平对比。

---

## 4.3.2 数据集与联邦划分

**数据集** 采用 **CIFAR-10**：50 000 张训练图像、10 000 张测试图像，10 类，尺寸 \(32\times 32\times 3\)。预处理与 `torchvision` 官方实践一致：先 **ToTensor**，再按通道做 **标准化**，均值与方差取 CIFAR-10 常用统计量 \((0.4914, 0.4822, 0.4465)\) 与 \((0.2023, 0.1994, 0.2010)\)。**未使用** 随机裁剪、颜色抖动等额外数据增强，以便突出 **聚合算法** 本身对非 IID 数据的影响。

**客户端数量** 取 \(K=10\)。训练集采用 **Dirichlet 数量划分** 构造标签分布倾斜的非 IID 场景：对每一类别 \(c\)，将其样本索引在客户端间按参数为 \(\beta\) 的 Dirichlet 分布抽样分配；\(\beta\) 越小，各客户端标签分布越不均衡。第 4 章系统对比中，对 \(\beta\in\{0.05,0.1,0.3,0.5,1.0\}\) 分别重复整套实验（与 `experiments/comparison_aligned.py --betas ...` 一致）；单组默认配置中亦取 \(\beta=0.5\) 作为中等非 IID 设定（`configs/default.yaml` / `configs/ch4_compare.yaml` 中 `client.dirichlet_beta`）。

**代理集**：从训练集中按 **`proxy_ratio=0.04`** 抽取 **4%** 样本构成服务器端代理数据；且设置 **`proxy_disjoint_from_clients: true`**，保证代理样本索引与 **所有** 客户端本地训练索引 **互不相交**，避免信息泄漏，与论文设定一致。

---

## 4.3.3 网络结构

全局模型与各客户端本地模型均为同一结构的 **轻量级卷积网络 `SimpleCNN`**（见 `models/models.py`）：三层 \(3\times 3\) 卷积（通道 \(3\!\to\!32\!\to\!64\!\to\!64\)），每层后接 ReLU 与 \(2\times 2\) 最大池化，展平后经全连接层 \(1024\to 256\to 10\)（CIFAR-10 输入 \(32\times 32\) 时中间特征维为 \(64\times 4\times 4=1024\)）。该网络参数量约 **\(3.3\times 10^5\)** 量级（约 **0.35 M**），便于在联邦设定下快速完成多轮、多 \(\beta\) 的对比实验。

---

## 4.3.4 联邦学习与优化超参数

**通信轮数** 上限取 \(T_{\max}=30\)（`server.global_rounds`），与第 4.2 节中「达到目标准确率所需轮次」的统计上限一致。

**客户端本地训练**：优化器为 **SGD**，学习率 **`learning_rate=0.01`**，动量 **`momentum=0.9`**；每轮通信内 **本地 epoch 数 `local_epochs=2`**；本地小批量大小 **`batch_size=128`**。实现上对训练集 `DataLoader` 使用 **`drop_last=True`**，避免 BatchNorm 在 batch 大小为 1 时训练前向报错。

**FedProx 对比**：近端项系数取 **`fedprox_mu=0.01`**（与 `comparison_aligned.py` 中 FedProx 分支一致）。

**服务端（本文可学习聚合）**：权重 logits 的优化器为 **SGD**，学习率 **`aggregation_weight_lr=0.01`**，动量 **`momentum=0.9`**；代理集上前向 batch 大小 **`proxy_batch_size=64`**；向均匀分布收缩系数 **`lam=0.1`**；客户端凸组合系数之和 **`shrink_sum=1.0`**（即本轮更新为客户端参数的凸组合，无额外「和&lt;1」收缩）。FedAvg / FedProx 分支使用相同客户端超参，仅聚合方式不同。

**全局测试**：每轮结束后在 **完整测试集** 上计算 Top-1 准确率；评测时 **`eval_batch_size=256`**。

**设备**：配置中 **`device: auto`**（或按环境改为 `cuda`），由程序自动选择 GPU（若可用）。

---

## 4.3.5 对比方法与复现命令

第 4 章在同一初始化、同一划分下依次运行 **FedAvg**、**FedProx** 与 **本文自适应（可学习）聚合** 三种方法，并汇总各 \(\beta\) 下的准确率曲线及达到阈值 **50%、55%、60%** 的首个通信轮次（见第 4.2 节）。

在项目根目录执行（建议在 `screen` / `tmux` 中后台运行）：

```bash
python experiments/comparison_aligned.py configs/ch4_compare.yaml \
  --betas 0.05 0.1 0.3 0.5 1.0 \
  --thresholds 50,55,60
```

主要输出包括：`experiments/outputs/` 下各 \(\beta\) 的 **准确率**、**测试集平均交叉熵**、**代理集 meta-loss**（仅 Adaptive 有值）对比图（`comparison_acc_*.png`、`comparison_loss_*.png`、`comparison_proxy_loss_*.png`），同名 `.npz`（字段含 `acc_*`、`test_loss_*`、`proxy_loss_*`），以及汇总表 **`ch4_comparison_summary.csv`**。若需单独训练本文方法并保存全局权重，可使用：

```bash
python main.py configs/ch4_compare.yaml
```

权重默认路径见配置项 **`output.model_path`**（第 4 章对比中为 `weights/ch4/adaptive_cifar10_dirichlet.pth`）。同一次运行生成的 **`adaptive_*_run_log.npz`** 中含每轮 **`accuracy`**、**`test_loss`**（测试集样本平均交叉熵）与 **`proxy_loss`**（服务端可学习聚合一步前的代理集 meta-loss），可用 `python utils/plot_run_log.py <npz路径>` 绘图。

---

## 4.3.6 与参考论文体例的对应说明

参考学位论文中常见的实验细节写法：本节先给出 **计算平台与框架版本**，再给出 **数据与划分**、**模型与优化器超参**，最后说明 **对比对象与运行入口**。本文任务为 **联邦图像分类**，未涉及医学图像滑窗采样、多模态融合等设定；网络为 **轻量 CNN** 而非大型 Transformer，与课题代码实现一致，便于在租用算力上完成可重复的消融与对比。
