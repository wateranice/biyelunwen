# 第 3 章 3.3 元梯度驱动的聚合权重演化

本节在**服务端、代理集监督**的前提下，给出聚合权重的 **Softmax 参数化**、**元损失** \(\mathcal{L}_p\) 的严格定义，以及 \(\mathcal{L}_p\) 对隐变量 \(\boldsymbol{z}\)（实现中的 `weight_logits`）的**链式梯度**与优化步。因果链为  
\[
\boldsymbol{z}\ \Rightarrow\ \boldsymbol{u}=\mathrm{softmax}(\boldsymbol{z})\ \Rightarrow\ \tilde{\boldsymbol{\theta}}(\boldsymbol{u})\ \Rightarrow\ \mathcal{L}_p\ \Rightarrow\ \nabla_{\boldsymbol{z}}\mathcal{L}_p\ \Rightarrow\ \boldsymbol{z}\ \text{更新}.
\]  
为突出**梯度驱动**机制，下文在可微路径上把「经 Softmax 后参与前向的混合参数」写为各客户端上传参数的**单纯形凸组合**；实现上通过 `torch.func.functional_call`（或兼容分支 `stateless.functional_call`）在**不改变各 `client_models` 存储张量**的前提下，对聚合态做一次前向与反传。本节**不讨论**与本条梯度链无直接关系的权值后处理，那些留待他节；此处 \(\boldsymbol{u}\) 始终落在概率单纯形 \(\Delta^{K-1}\) 上。

---

## 3.3.1 符号与 Softmax 参数化权值

设本轮通信结束后，服务端已收集 \(K\) 个客户端上传的本地参数副本，记第 \(k\) 个客户端的全网一致参数化（逐层张量拼接的抽象记号为）\(\boldsymbol{\theta}_k\)，\(k=1,\ldots,K\)。服务端维护可训练向量  
\[
\boldsymbol{z}=(z_1,\ldots,z_K)^\top\in\mathbb{R}^K,
\]  
并通过 Softmax 生成聚合权  
\[
u_i=\frac{\mathrm{e}^{z_i}}{\sum_{j=1}^{K}\mathrm{e}^{z_j}},\qquad i=1,\ldots,K,
\]  
记 \(\boldsymbol{u}=\mathrm{softmax}(\boldsymbol{z})\)，则 \(\boldsymbol{u}\in\Delta^{K-1}\)，且 \(\sum_i u_i=1\)。对任一网络参数张量 \(m\)（例如某卷积核或偏置），定义**混合参数**为  
\[
\tilde{\boldsymbol{\theta}}_m(\boldsymbol{u})=\sum_{k=1}^{K} u_k\,\boldsymbol{\theta}_{k,m},
\]  
其中 \(\boldsymbol{\theta}_{k,m}\) 为客户端 \(k\) 上第 \(m\) 个可学习张量。将所有 \(m\) 按模块接口装配后，记整体混合权重为 \(\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\)。**Softmax 参数化**把「离散选谁」松弛为单纯形上的光滑选择，使 \(\boldsymbol{u}\)（从而 \(\boldsymbol{z}\)）可通过梯度从代理损失中获得更新信号。

---

## 3.3.2 基于代理集的元损失 \(\mathcal{L}_p\)

设代理集 \(\mathcal{D}_p=\{(x^{(b)},y^{(b)})\}_{b=1}^{B}\) 在一轮内按 mini-batch 遍历（实现中可对各 batch 损失取算术平均）。记网络在参数 \(\tilde{\boldsymbol{\theta}}\) 下对输入 \(x\) 的 logits 为 \(f(x;\tilde{\boldsymbol{\theta}})\)。采用多类交叉熵时，**经验元损失**定义为  
\[
\mathcal{L}_p(\boldsymbol{z})=\frac{1}{B}\sum_{b=1}^{B}\ell_{\mathrm{CE}}\!\Big(f\big(x^{(b)};\tilde{\boldsymbol{\theta}}(\boldsymbol{u}(\boldsymbol{z}))\big),\,y^{(b)}\Big),
\]  
其中 \(\boldsymbol{u}(\boldsymbol{z})=\mathrm{softmax}(\boldsymbol{z})\)，\(\ell_{\mathrm{CE}}\) 为 softmax–交叉熵复合函数。  
**元梯度**一词的含义在于：\(\boldsymbol{z}\) 并不直接最小化任一客户端私有目标，而是通过「在代理数据上评价混合模型 \(\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\)」这一**外层**指标，把各 \(\boldsymbol{\theta}_k\) 当作本轮固定的系数，只对 \(\boldsymbol{z}\) 求导并更新——客户端参数在此扮演**外层问题的结构参数**，\(\boldsymbol{z}\) 扮演**内层可优化变量**。

---

## 3.3.3 链式法则：从 \(\mathcal{L}_p\) 到 \(\boldsymbol{z}\)

记 \(\boldsymbol{\theta}(\boldsymbol{u})\equiv \tilde{\boldsymbol{\theta}}(\boldsymbol{u})\) 为拼接后的混合参数向量（按展平记法便于书写）。对单样本（省略 batch 上标），链式法则给出  
\[
\frac{\partial \mathcal{L}_p}{\partial z_j}
=\sum_{i=1}^{K}\frac{\partial \mathcal{L}_p}{\partial u_i}\,\frac{\partial u_i}{\partial z_j}.
\]  
下面分别推导 \(\partial u_i/\partial z_j\) 与 \(\partial \mathcal{L}_p/\partial u_i\)。

### （1）Softmax 的 Jacobian

对 \(u_i=\mathrm{e}^{z_i}/Z\)，\(Z=\sum_r \mathrm{e}^{z_r}\)，有  
\[
\frac{\partial u_i}{\partial z_j}
=u_i(\delta_{ij}-u_j),
\]  
其中 \(\delta_{ij}\) 为 Kronecker 记号。写成矩阵形式：\(J_{ij}=\partial u_i/\partial z_j=u_i(\delta_{ij}-u_j)\)。

### （2）损失对 \(\boldsymbol{u}\) 的偏导

混合参数对 \(u_i\) 是**线性**的：对每个张量 \(m\)，\(\tilde{\boldsymbol{\theta}}_m=\sum_k u_k \boldsymbol{\theta}_{k,m}\)，故  
\[
\frac{\partial \tilde{\boldsymbol{\theta}}_m}{\partial u_i}=\boldsymbol{\theta}_{i,m}.
\]  
设 \(\mathcal{L}_p\) 对 \(\tilde{\boldsymbol{\theta}}_m\) 的（欧氏内积意义下的）梯度张量为 \(\mathbf{G}_m\equiv \nabla_{\tilde{\boldsymbol{\theta}}_m}\mathcal{L}_p\)（由交叉熵经网络反向传播得到）。则标量 \(\mathcal{L}_p\) 对 \(u_i\) 的导数为所有参数张量上的 Frobenius 内积之和：  
\[
\frac{\partial \mathcal{L}_p}{\partial u_i}
=\sum_{m}\Big\langle \mathbf{G}_m,\,\boldsymbol{\theta}_{i,m}\Big\rangle,
\]  
其中 \(\langle \mathbf{A},\mathbf{B}\rangle=\sum_{p,q,\ldots}A_{pq\ldots}B_{pq\ldots}\)。  
记  
\[
g_i \equiv \frac{\partial \mathcal{L}_p}{\partial u_i},
\]  
则向量 \(\boldsymbol{g}=(g_1,\ldots,g_K)^\top\) 完全由「代理损失对混合参数的梯度」与「各客户端张量」配对得到。

### （3）合成到 \(\boldsymbol{z}\)

将 (1)(2) 代入链式法则：  
\[
\frac{\partial \mathcal{L}_p}{\partial z_j}
=\sum_{i=1}^{K} g_i\,u_i(\delta_{ij}-u_j)
=u_j\Big(g_j-\sum_{i=1}^{K} g_i u_i\Big).
\]  
向量形式即  
\[
\nabla_{\boldsymbol{z}}\mathcal{L}_p
=\boldsymbol{u}\odot(\boldsymbol{g}-\mathbf{1}\,\boldsymbol{u}^\top\boldsymbol{g}),
\]  
其中 \(\odot\) 为逐分量乘积，\(\mathbf{1}\) 为全 1 向量，\(\boldsymbol{u}^\top\boldsymbol{g}=\sum_i g_i u_i\) 为标量。该式即为 softmax 层之后接「线性混合参数」时**标准且闭合**的反传结果：梯度在 \(\boldsymbol{g}\) 上做一次**以 \(\boldsymbol{u}\) 为权的去均值**，再按 \(u_j\) 缩放。

### （4）因果链：从梯度流到 \(\boldsymbol{z}\) 的更新

实现中对 \(\mathcal{L}_p\) 在代理 loader 上各 batch 累加后除以 batch 数，得到标量 \(\mathcal{L}_p\)，调用自动微分 \(\mathcal{L}_p.\texttt{backward}()\)。计算图路径为  
\[
\boldsymbol{z}\xrightarrow{\mathrm{softmax}}\boldsymbol{u}\xrightarrow{\text{线性混合}}\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\xrightarrow{f(\cdot)}\text{logits}\xrightarrow{\ell_{\mathrm{CE}}}\mathcal{L}_p,
\]  
PyTorch 按上述链式法则在反向模式中累积 \(\partial \mathcal{L}_p/\partial \boldsymbol{z}\)。随后优化器（实现为带动量的 SGD）对 `weight_logits` 执行  
\[
\boldsymbol{z}\leftarrow \boldsymbol{z}-\eta\,\nabla_{\boldsymbol{z}}\mathcal{L}_p\ +\ \text{(动量项)},
\]  
其中 \(\eta\) 为 `aggregation_weight_lr`。**因果顺序**严格为：先由代理监督产生 \(\mathcal{L}_p\)，再经反传得到 \(\nabla_{\boldsymbol{z}}\mathcal{L}_p\)，最后才触发 \(\boldsymbol{z}\) 的数值更新；前向中 \(\boldsymbol{\theta}_{k,m}\) 以 `detach` 方式进入混合（见实现），保证本轮元梯度**不修改**客户端上传副本，仅更新服务端 \(\boldsymbol{z}\)。

---

## 3.3.4 `functional_call` 与「虚拟评估」的实现含义

若直接在某个 `nn.Module` 实例上 `load_state_dict(\tilde{\boldsymbol{\theta}})\)`，会**覆写**存储并干扰后续逻辑。`functional_call(module, param_dict, args)` 的含义是：在**不修改** `module` 内部持久化参数的前提下，用 `param_dict` 中给出的张量替换前向所需的权重，完成一次** Stateless forward**。因此，对同一批代理图像 \(x^{(b)}\)，服务端可写  
\[
\texttt{logits}^{(b)} = f\big(x^{(b)};\,\tilde{\boldsymbol{\theta}}(\boldsymbol{u}(\boldsymbol{z}))\big)
\]  
而各 `client_models[k].state_dict()[m]` 仍保持为 \(\boldsymbol{\theta}_{k,m}\) 的本地副本。  
**「虚拟评估」**指：混合模型 \(\tilde{\boldsymbol{\theta}}(\boldsymbol{u})\) 并不持久化为某个独立 `Module` 实例，只在计算图中**临时实例化**前向；其评价指标 \(\mathcal{L}_p\) 的梯度经 `param_dict` 的依赖链回传到 \(\boldsymbol{z}\)。一旦 `backward` 完成，虚拟混合态即被释放，从而实现「试聚合—打分—反传—只改 \(\boldsymbol{z}\)」的闭环。

---

## 3.3.5 小结：元梯度视角下的权重演化

Softmax 将无约束 \(\boldsymbol{z}\) 映射到合法凸组合权 \(\boldsymbol{u}\)；代理元损失 \(\mathcal{L}_p(\boldsymbol{z})\) 在 \(\boldsymbol{u}\) 上诱导出 \(\boldsymbol{g}=\partial \mathcal{L}_p/\partial \boldsymbol{u}\)；再经 softmax 几何的 Jacobian 合成 \(\nabla_{\boldsymbol{z}}\mathcal{L}_p\)；最后由优化器更新 \(\boldsymbol{z}\)。整条链在自动微分上等价于手动公式  
\[
\frac{\partial \mathcal{L}_p}{\partial z_j}
=u_j\Big(\frac{\partial \mathcal{L}_p}{\partial u_j}-\sum_{i=1}^{K}u_i\frac{\partial \mathcal{L}_p}{\partial u_i}\Big),
\]  
与 3.3.3 节推导一致。由此，**权重演化**在本节意义下专指：\(\boldsymbol{z}\)（从而 \(\boldsymbol{u}\)）沿代理监督下的下降方向连续调整，使混合模型在 \(\mathcal{D}_p\) 上的分类误差驱动聚合倾向——**全程由梯度因果链刻画**，与任何事后非梯度式的权重整形无涉。

---

*（实现对应：`src/AdaptiveWeighted/server_aw.py` 中 `weight_logits`、`F.softmax`、`functional_call`、代理集交叉熵及对 `weight_logits` 的优化步；`param_dict` 中来自各客户端的张量以 `detach` 形式参与混合，阻断对客户端存储的元梯度。）*
