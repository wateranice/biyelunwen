# 论文章节 — 实验编号 — 命令 / 配置（任务书对照）

下表按 **`任务书.txt`** 要求，将「建议写入论文的实验」编号，并与 **论文章节**、**项目根目录下命令**、**YAML 配置** 对应。路径均以仓库根目录为当前工作目录（`cd /path/to/biyelunwen`）。

---

## 总览表

| 论文章节（建议落笔位置） | 编号 | 任务书对应要点 | 配置（YAML） | 命令（示例） |
|--------------------------|------|----------------|--------------|--------------|
| 第 3 章 数据与联邦环境 | E3-1 | 代理数据 1%～5%、与客户端关系 | `configs/ch4_compare.yaml`：`client.proxy_ratio`、`proxy_disjoint_from_clients` | 无独立命令；随 E4-1/E4-2 等 `prepare_data` 自动生效 |
| 第 3 章 数据与联邦环境 | E3-2 | Dirichlet Non-IID | 同上：`client.partition: dirichlet`，`client.dirichlet_beta` | 随 E4-1 中 `--betas` 覆盖 `dirichlet_beta` |
| 第 3 章 数据与联邦环境 | E3-3 | 病态（Pathological）划分 | **新建** `configs/ch4_pathological.yaml`：复制 `ch4_compare.yaml` 后设 `client.partition: pathological` | `python experiments/comparison_aligned.py configs/ch4_pathological.yaml --betas 0.1 0.5 --thresholds 50,55,60` |
| 第 4 章 对比实验 | E4-1 | 同 Non-IID 下 FedAvg / FedProx / 本文对比；多异构强度 | `configs/ch4_compare.yaml` | `python experiments/comparison_aligned.py configs/ch4_compare.yaml --betas 0.05 0.1 0.3 0.5 1.0 --thresholds 50,55,60` |
| 第 4 章 对比实验 | E4-2 | 任务书「三数据集」：MNIST | **新建** `configs/ch4_mnist.yaml`：复制 `ch4_compare.yaml`，`experiment.dataset: mnist`，`output.model_path` 改到 `weights/ch4/...mnist...` | `python experiments/comparison_aligned.py configs/ch4_mnist.yaml --betas 0.05 0.1 0.5 1.0 --thresholds 50,55,60` |
| 第 4 章 对比实验 | E4-3 | 任务书「三数据集」：Fashion-MNIST | **新建** `configs/ch4_fmnist.yaml`：`experiment.dataset: fmnist`，`output.model_path` 指向 fmnist | `python experiments/comparison_aligned.py configs/ch4_fmnist.yaml --betas 0.05 0.1 0.5 1.0 --thresholds 50,55,60` |
| 第 4 章 评价指标与曲线 | E4-4 | 测试集 **Accuracy**；达阈值 **通信轮次** | 同 E4-1 配置 + 命令行 `--thresholds` | 含于 E4-1；产出 `experiments/outputs/ch4_comparison_summary_{dataset}_{partition}.csv`（如 `..._cifar10_dirichlet.csv`、`..._mnist_dirichlet.csv`） |
| 第 4 章 评价指标与曲线 | E4-5 | **Loss vs. 通信轮次**（任务书 4.(1)） | 同对比用 yaml | **已实现**：每轮为 **测试集样本平均交叉熵**（nats）。`comparison_aligned.py` 在终端打印并在 `comparison_*_*.npz` 中存 `test_loss_fedavg` / `test_loss_fedprox` / `test_loss_adaptive`，另存 `comparison_loss_beta*.png`；`main.py`（`fl_system.py`）在 `*_run_log.npz` 中存 `test_loss`，可用 `python utils/plot_run_log.py <npz路径>` 生成 `_test_loss_from_log.png`。 |
| 第 4 章 权重行为分析 | E4-6 | 聚合权重随轮次可视化 | `configs/ch4_compare.yaml`（或各数据集 yaml） | `python main.py configs/ch4_compare.yaml` → 生成 `weights/ch4/*_run_log.npz`；再 `python utils/plot_run_log.py weights/ch4/adaptive_cifar10_dirichlet_run_log.npz` |
| 第 4 章 消融 / 敏感性 | E4-7 | **代理数据规模**（1%～5% 及扩展） | `experiments/ablation_thesis.py` 内 `PRESETS` 或改 yaml 中 `proxy_ratio` | `python experiments/ablation_thesis.py configs/ch4_compare.yaml`；子集 `... --only baseline,proxy_001`；快速试跑加 `--rounds-override 2`；`--list` 列出预设 |
| 第 4 章 消融 / 敏感性 | E4-8 | **收缩系数** `shrink_sum`、与均匀混合 `lam`（任务书 2.(3)、4.(3)） | **新建** `configs/ch4_shrink.yaml`：复制 `ch4_compare.yaml`，扫 `server.shrink_sum`（如 `0.85, 0.9, 1.0`）；或扩展 `ablation_thesis.PRESETS` | 多组：`python main.py configs/ch4_shrink_0p9.yaml` 或对 `ablation_thesis` 增加 `("shrink_090", {"shrink_sum": 0.9})` 后 `python experiments/ablation_thesis.py configs/ch4_compare.yaml` |
| 第 4 章 机制分析 | E4-9 | 「权重和 < 1」收缩与泛化/正则（任务书 1、2.(3)） | 与 E4-8 同一批 `shrink_sum<1` vs `=1` 的 **测试精度 + 可选 Loss 曲线** 对照 | 同 E4-8；论文中结合 E4-5 曲线论述 |

---

## 与任务书条款的逐条映射（便于答辩）

| 任务书位置 | 实验编号（建议全做或选做标注） |
|------------|--------------------------------|
| 三数据集 MNIST / CIFAR-10 / Fashion-MNIST | **必做**：E4-1（CIFAR-10）+ E4-2 + E4-3 |
| Dirichlet 与病态划分 | **必做**：E3-2（含于 E4-1）+ **建议**：E3-3 |
| 代理 1%～5% | **必做**：E3-1（配置）+ **建议**：E4-7 |
| FedAvg、FedProx 对比 | **必做**：E4-1（及 E4-2/E4-3/E3-3 中同样 `--methods` 默认三方法） |
| Accuracy + **Loss–轮次** | Acc：**E4-4**；Loss：**E4-5**（已实现，见上表） |
| 权重可视化 | **必做**：E4-6 |
| 代理规模、收缩系数敏感性与消融 | **必做**：E4-7、E4-8、E4-9 |

---

## 一键顺序（服务器 / Linux）

与仓库中脚本一致（可只跑其中几步，见脚本内 `SKIP_*`）：

```bash
bash scripts/run_thesis_experiments.sh
```

对应关系简述：

| 脚本步骤 | 实验编号 |
|----------|----------|
| `comparison_aligned.py ... ch4_compare.yaml` | E4-1、E4-4（及 E3-1、E3-2） |
| `main.py configs/ch4_compare.yaml` | E4-6 |
| `ablation_thesis.py configs/ch4_compare.yaml` | E4-7（及部分 E4-8，视 `PRESETS` 是否含 `shrink_sum`） |

---

## 新建 YAML 时的最小修改清单（复制 `configs/ch4_compare.yaml`）

| 目的 | 修改字段 |
|------|----------|
| MNIST（E4-2） | `experiment.dataset: mnist`；`output.model_path` 避免覆盖 CIFAR 权重 |
| Fashion-MNIST（E4-3） | `experiment.dataset: fmnist`；同上改 `model_path` |
| 病态划分（E3-3） | `client.partition: pathological`；建议 `num_clients <= 10` |
| 收缩实验（E4-8/E4-9） | `server.shrink_sum: 0.9`（示例）；文件名区分如 `ch4_shrink0p9.yaml` |

---

## 产物速查（写论文时填表）

| 编号 | 主要产物路径（默认 CIFAR、ch4_compare） |
|------|----------------------------------------|
| E4-1 | `comparison_acc_*.png`、`comparison_loss_*.png`、`comparison_proxy_loss_*.png`、`*.npz`（`acc_*`、`test_loss_*`、`proxy_loss_*`）、`ch4_comparison_summary_{dataset}_{partition}.csv` |
| E4-6 | `weights/ch4/adaptive_cifar10_dirichlet.pth`、`adaptive_cifar10_dirichlet_run_log.npz`（含 `test_loss`）、plot 输出目录（含 `_test_loss_from_log.png`） |
| E4-7 | `weights/ch4/abl_*.pth`、各组 `*_run_log.npz`（若 `main` 式逻辑未改则以前缀 `abl_` 权重为准，以实际脚本为准） |

**说明：** `ablation_thesis.py` 调用的是 `FederatedLearningSystem`，权重名为 `abl_<tag>.pth`；`plot_run_log` 需指向带 `_run_log.npz` 的文件，请跑完后用 `ls weights/ch4/` 确认实际文件名。

---

*本文档随仓库维护。*
