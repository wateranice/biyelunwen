# 4.3 实验参数配置

本章实验在统一代码框架下读取 YAML 配置（默认见 `configs/default.yaml`；第 4 章正式对比可与 `configs/ch4_compare.yaml` 对齐，二者在**核心超参**上一致，差异主要为 `device` 与权重保存路径）。表 4-1 汇总**主实验**所采用的关键符号、取值及配置字段，便于与实现及可复现脚本对照。表中狄利克雷浓度记为 \(\alpha\)，与配置文件中的 **`dirichlet_beta`** 一一对应（下文统一称「狄利克雷参数 \(\alpha\)」）。

---

## 表 4-1 联邦学习主超参数总表（对应 `configs/default.yaml`）

| 符号 | 含义 | 取值 | YAML 路径（字段） |
|:----:|------|:----:|-------------------|
| \(K\) | 参与训练的客户端总数 | 10 | `client.num_clients` |
| \(T\) | 通信轮次（全局聚合轮数） | 30 | `server.global_rounds` |
| \(E\) | 每轮通信内各客户端**本地 epoch 数** | 2 | `client.local_epochs` |
| \(B\) | 客户端本地训练小批量大小 | 128 | `client.batch_size` |
| \(\eta\) | 客户端本地优化器（SGD）学习率 | 0.01 | `client.learning_rate` |
| \(\mu_{\mathrm{opt}}\) | SGD 动量系数 | 0.9 | `client.momentum` |
| \(B_{\mathrm{eval}}\) | 全局测试集评测 batch | 256 | `experiment.eval_batch_size` |
| \(p\) | 代理集占**训练集**比例 | 0.04（4%） | `client.proxy_ratio` |
| — | 代理样本是否与各客户端训练索引**集合互斥** | 是（`true`） | `client.proxy_disjoint_from_clients` |
| \(B_p\) | 服务端在代理集上更新聚合权重时的 batch | 64 | `server.proxy_batch_size` |
| \(\eta_p\) | 服务端聚合权重 logits 的 SGD 学习率 | 0.01 | `server.aggregation_weight_lr` |
| \(\lambda\) | 聚合权重向均匀分布的**平滑因子**（凸组合系数） | 0.1 | `server.lam` |
| \(s\) | **收缩系数**：各客户端凸组合系数之和（`shrink_sum`） | 1.0 | `server.shrink_sum` |
| \(\alpha\) | 狄利克雷划分浓度参数（Non-IID 程度，**默认主实验**） | 0.5 | `client.dirichlet_beta` |
| — | 数据划分方式（默认） | Dirichlet | `client.partition` |
| \(\mu\) | FedProx 近端项系数（仅 FedProx 基线） | 0.01 | `client.fedprox_mu` |
| — | 随机种子 | 42 | `experiment.seed` |

**说明：** \(s=1.0\) 表示本轮混合更新中客户端系数之和为 1，**不显式混入「和 &lt; 1」的全局锚点收缩**；任务书要求的收缩效应可通过另取 \(s&lt;1\) 的消融实验讨论（见 `ablation_thesis.py` 预设）。**设备** `server.device` 在 `default.yaml` 中为 `cuda`，在 `ch4_compare.yaml` 中为 `auto`，依运行环境自动选择 GPU/CPU。

---

## 4.3.1 参数选择依据

**（1）客户端规模 \(K\) 与通信轮次 \(T\)**  
取 \(K=10\) 与 CIFAR-10 的 10 类结构相匹配，便于在**病态划分**（`pathological`）下每客户端至少能覆盖合理类别子集，且单机/单卡复现成本可控。\(T=30\) 与任务书及 `ch4_compare.yaml` 中「正式对比」轮数一致，足以观察收敛趋势并统计达到目标准确率所需轮次；快速试跑可通过命令行 `--rounds-override` 缩短，不改动表 4-1 主设定。

**（2）本地训练 \((E,B,\eta,\mu_{\mathrm{opt}})\)**  
\((E,B)=(2,128)\) 为联邦图像分类中常用配置：在**每轮通信**内完成有限次本地更新即可上传模型，避免本地过拟合于极小批量；\(B=128\) 在 CIFAR-10 与所用 CNN 容量下兼顾显存与梯度噪声。\(\eta=0.01\)、\(\mu_{\mathrm{opt}}=0.9\) 为 SGD 在类似视觉任务上的稳定组合，并与 FedProx 等基线文献中常见量级对齐，便于公平对比。

**（3）代理集比例 \(p\) 与互斥设定**  
任务书建议代理数据约占训练集的 **1%～5%**。取 **\(p=0.04\)** 处于该区间中段，在「信息量」与「与客户端数据重叠风险」之间折中。`proxy_disjoint_from_clients: true` 保证代理索引与各客户端本地索引**互不相交**，避免将同一批样本既用于本地训练又用于服务端权重学习，从而更符合「服务端仅持有一小份**独立**代理数据」的设定。

**（4）平滑因子 \(\lambda\) 与收缩 \(s\)**  
\(\lambda=0.1\) 控制 Softmax 聚合权重经 SGD 更新后向**均匀混合**的凸组合强度，防止权重过度尖锐导致某一客户端主导聚合。\(\lambda\) 与 FedProx 中 \(\mu\) 符号不同：后者为**客户端近端项**，见表 4-1 末行。\(s=1.0\) 为主实验默认，对应**无额外全局锚点收缩**；探究「和 &lt; 1」正则时改为 \(s\in(0,1)\) 即可（与 `server.shrink_sum` 注释一致）。

**（5）狄利克雷参数 \(\alpha\)（`dirichlet_beta`）**  
**表 4-1** 给出**单组主配置** \(\alpha=0.5\)，表示中等程度的标签分布倾斜。第 4 章系统对比中，在**相同初始化与相同其它超参**下对 \(\alpha\in\{0.05,0.1,0.3,0.5,1.0\}\) 进行扫描：**\(\alpha\) 越小**，客户端间类别分布越不均衡（**更强 Non-IID**），用于检验自适应聚合在**高度偏斜**场景下的优势；**\(\alpha\) 较大**时接近 IID 客户端，用于观察各方法在较温和异构下的差距缩小趋势。

**（6）随机种子**  
`seed=42` 固定数据划分、Dirichlet 抽样、代理子集抽取（在互斥模式下与 `torch.Generator` 一致）及模型初始化相关随机性，保证 **FedAvg、FedProx 与本文方法** 在同一实验脚本中可复现、可比。

**（7）与 `ch4_compare.yaml` 的关系**  
第 4 章对比脚本以 `ch4_compare.yaml` 为入口时，**表 4-1 中除 \(T\) 所依赖的 device 与输出路径外的字段与 `default.yaml` 一致**；多 \(\alpha\) 由命令行 `--betas` 覆盖 `dirichlet_beta`，不改变其余默认，从而保证「仅改变 Non-IID 强度」的单一变量原则。

---

*注：若论文中「4.3」已用于实验环境与平台描述，可将本节作为 **4.3.1 超参数** 插入，或将硬件与软件环境单独列为 **4.2** / **附录**，以保持章节编号与学校模板一致。*
