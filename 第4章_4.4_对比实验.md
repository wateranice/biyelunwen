# 4.4 对比实验

本节在 **统一的联邦学习协议** 下，将本文提出的 **基于服务端代理数据的可学习自适应聚合**（下文记为 **Adaptive** 或 **本文方法**）与两类经典基线算法 **FedAvg**、**FedProx** 进行对比。实验采用相同的随机种子、相同的数据集与划分接口、相同的客户端本地训练超参（见 4.3 节表 4-1），仅替换 **服务端聚合方式**，从而保证对比的公平性。评价指标以 **全局测试集 Top-1 准确率（Acc）随通信轮次的变化曲线** 为主，并在多个 **狄利克雷浓度参数 \(\alpha\)**（与配置项 `dirichlet_beta` 一致）下重复实验，以覆盖 **由弱到强** 的 Non-IID 场景。

---

## 4.4.1 实验设置与对比对象

**数据集与模型。** 主实验在 **CIFAR-10** 上进行；全局模型与各客户端本地模型均为同一结构的轻量级卷积网络（见 4.3 节）。全局测试集在各通信轮次结束后进行 **IID 评测**。

**Non-IID 设定。** 训练集在客户端间按 **Dirichlet 数量划分** 分配：对每个类别，将其样本索引在 \(K\) 个客户端间按参数为 \(\alpha\) 的狄利克雷分布抽样划分。**\(\alpha\) 越小**，各客户端上的 **类别分布越不均衡**，客户端间 **Non-IID 程度越强**；**\(\alpha\) 较大** 时，各客户端更接近 **类均衡的 IID 客户端**。本文在 \(\alpha \in \{0.05, 0.1, 0.3, 0.5, 1.0\}\) 等多组取值下分别运行完整对比（与 `experiments/comparison_aligned.py` 的 `--betas` 扫描一致），其中 \(\alpha=0.05\) 对应 **高度偏斜** 的极端异构场景，\(\alpha=1.0\) 对应相对温和的异构。

**对比算法。**

- **FedAvg**：对各客户端上传模型参数做 **均匀加权平均**，不利用服务端代理数据。
- **FedProx**：在客户端目标中引入 **近端项** 以抑制本地漂移，服务端仍采用 **均匀聚合**；超参 \(\mu\) 与配置文件一致。
- **本文方法（Adaptive）**：在服务端维护 **可学习的聚合权重**，在 **代理数据集** 上对「混合后的全局参数」所给出的分类损失关于权重做 **梯度型更新**，再经平滑因子 \(\lambda\) 向均匀分布收缩并完成全局写回（见第 3 章）。

**通信与评测。** 每完成一轮「本地训练—上传—聚合」，记为 **1 个通信轮次**。每轮结束后在完整测试集上计算 Acc，并绘制 **Acc–通信轮次** 曲线；可同时参考同脚本输出的 **测试集平均交叉熵（Loss）** 与 **代理集 meta-loss** 曲线，用于辅助理解收敛行为（见 4.2 节指标定义）。

---

## 4.4.2 准确率曲线对比

图 4-*（由 `experiments/outputs/comparison_acc_beta{α}_cifar10_dirichlet.png` 系列汇总）给出了在 **不同 \(\alpha\)** 下，三种方法在同一坐标系中的 **测试 Acc 随通信轮次** 的变化。

**总体形态。** 在各类 Non-IID 强度下，三条曲线均随轮次 **单调或波动上升** 并趋于饱和，符合联邦学习在 IID 测试集上的典型收敛轮廓。差异主要体现在 **上升斜率（早期收敛速度）**、**中期平台高度** 以及 **末轮或饱和区精度** 三方面。

**FedAvg。** 在 **\(\alpha\) 较小、异构较强** 时，由于各客户端梯度方向差异大且聚合权重 **固定为均匀**，全局模型在客户端间 **「折中」** 的倾向更明显，常表现为 **早期上升较慢**、**震荡相对明显**，末轮精度 **相对偏低**。当 \(\alpha\) 增大、客户端分布更接近 IID 时，FedAvg 的性能 **逐步改善**，与经典观察一致。

**FedProx。** 近端项在一定程度上 **约束本地更新不要偏离全局过远**，在强异构下通常 **优于或接近 FedAvg**，表现为曲线 **更平滑** 或 **相同轮次内 Acc 更高**。但在 **服务端仍缺乏「谁更可信」的细粒度区分** 时，其提升存在 **天花板**；当异构极强时，仍可能出现 **收敛偏慢** 或 **最终精度不及充分利用代理监督的方法**。

**本文方法（Adaptive）。** 在多数 \(\alpha\) 设置下，曲线整体位于 FedAvg/FedProx **上方或更早进入高 Acc 区间**：即在 **相同通信预算** 下，往往能以 **更少的轮次** 达到同等测试精度，或在 **相同轮次** 下取得 **更高的末轮精度**。在 **\(\alpha\) 较小** 的困难设定下，这一优势通常 **更为显著**。

---

## 4.4.3 Non-IID 程度加剧（\(\alpha\) 减小）时的表现差异

当 **\(\alpha\) 从大变小** 时，客户端标签分布 **更加分散与倾斜**：部分客户端 **主导类集中**、部分类在多数客户端上 **长期稀缺**，导致 **局部最优方向与全局目标冲突加剧**。此时：

- **FedAvg** 的均匀聚合更易放大 **「客户端漂移」**，表现为 **同轮次 Acc 更低、曲线更抖**；
- **FedProx** 通过近端项 **缓解漂移**，但 **未显式度量** 各客户端模型对「全局一致目标」的贡献差异；
- **本文方法** 通过 **代理集上的可学习权重**，在每一轮聚合前 **自适应地提高** 对当前全局目标更有利的客户端组合权重、**抑制** 与代理监督不一致的极端更新，因此在 **\(\alpha\) 减小、异构加剧** 时，**相对优势往往扩大**。

换言之：**异构越强，「谁来主导聚合」越重要**；固定均匀权重或仅靠近端正则，均不如 **利用少量共享代理数据给出的显式监督信号** 来得直接。

---

## 4.4.4 最终准确率与收敛速度（与任务书表述对齐）

从 **最终准确率** 看：在中小 \(\alpha\) 的强异构区间，本文方法在 **末轮或接近饱和轮次** 的测试 Acc **通常高于** FedAvg，并 **常不低于、在多数设定下优于** FedProx；在较大 \(\alpha\) 的温和异构区间，三者差距 **趋于缩小**，但本文方法仍常保持 **不慢于基线的收敛尾部**。

从 **收敛速度** 看：以 4.2 节定义的 **首次达到目标准确率阈值** \(T\in\{50\%,55\%,60\%\}\) 所需的通信轮次 \(R(T)\) 为量化指标（见 `ch4_comparison_summary_{dataset}_{partition}.csv`），在 **\(\alpha\) 较小** 时，本文方法对应的 \(R(T)\) **往往更小**，即 **更快达到同等精度**，体现为 **Acc 曲线更早进入高平台区**。这与「减少通信轮次、降低通信开销」的联邦学习目标一致。

---

## 4.4.5 从代理集监督角度的机理解释

本文方法的核心在于：服务器端持有一小部分 **与客户端训练索引互斥** 的 **代理数据** \(\mathcal{D}_p\)，并在每轮聚合时将各客户端模型以 **可学习系数** 凸组合后，在 \(\mathcal{D}_p\) 上评估 **分类损失** 并 **对聚合权重做梯度更新**。其直观含义是：

1. **代理集提供「全局一致」的参照坐标系**  
   各客户端本地数据分布各异，**单独看本地损失** 难以判断哪一客户端更新更有利于 **全局测试泛化**；而 \(\mathcal{D}_p\) 在实现上来自 **同一训练分布的独立子样本**，为服务器提供了一个 **跨客户端可比** 的评估窗口。

2. **权重学习等价于在代理风险上选择更优的凸组合**  
   当 Non-IID 加剧时，「平均即公平」不再成立：某些客户端更新会 **系统性拉低** 代理集上的拟合质量。可学习权重通过 **降低** 这些更新在凸组合中的占比、**提高** 对代理风险下降更有效的客户端占比，使聚合后的全局模型 **更快沿着有利于 \(\mathcal{D}_p\) 上损失下降的方向前进**；在 IID 测试集与训练分布一致的前提下，这通常与 **测试 Acc 提升** 同向。

3. **与 FedProx 的互补视角**  
   FedProx 主要在 **客户端内部** 用近端项抑制漂移；本文方法在 **服务器端** 用代理监督 **显式选择** 聚合方向。二者可同时存在（对比脚本中 FedProx 为独立基线），从机理上说明：**强异构下既需要「别走太远」，也需要「知道该听谁的」**。

综上，4.4 节的对比实验从 **曲线形态、末轮精度、达到阈值轮次** 三个层面展示：在 **Non-IID 加剧（\(\alpha\) 减小）** 时，本文方法相对 FedAvg/FedProx 在 **最终精度与收敛速度** 上具有 **稳定优势**；该优势可归结为 **代理集监督下的自适应聚合** 在服务器端提供了 **对全局一致目标更敏感** 的学习信号。

---

**写作提示（待实验填数）：** 正式定稿时，将上文中的「通常 / 往往 / 多数设定」替换为与你 **`ch4_comparison_summary_*_*.csv`** 及 **`comparison_acc_beta*.png`** 一致的 **定量措辞**（例如给出具体末轮 Acc 与 \(R(60)\) 对比），并在图注中写明 **\(\alpha\)、\(K\)、\(T\)** 与文件名，以保证可复核。
