# 第 3 章 3.1 问题形式化（中英文稿，供润色）

> 说明：下文根据本仓库实现整理（`fl_system.py`、`dataset/data_utils.py`、`src/AdaptiveWeighted/server_aw.py`、`src/client_base.py`）。可直接整份发给 Gemini 做语言与结构优化；优化时请保持符号与代码含义一致。

---

## 中文版

### 3.1 问题形式化

#### 3.1.1 符号与集合

考虑含 \(K\) 个客户端的横向联邦图像分类。第 \(t\) 轮通信（\(t=1,\ldots,T\)）开始前，服务端持有全局模型参数 \(w_t\)。客户端 \(k\) 的私有训练索引集合记为 \(\mathcal{I}_k\)，其经验分布由本地子集 \(\mathcal{D}_k=\{(x,y)\}\) 刻画；全体客户端私域不交叠，\(\mathcal{D}_k\) 之间不交换原始样本。训练集经划分后另取一小部分作为服务端可见的代理集 \(\mathcal{D}_p\)，实现上由 `prepare_data` 在完整训练集上按比例 `proxy_ratio` 做随机子集划分得到，与 `client_id_map` 所给出的各客户端索引并行存在；代理集标签在服务端用于监督信号，不下发给客户端。

客户端 \(k\) 在第 \(t\) 轮以学习率 \(\eta\)、动量系数（与实现一致）在本地数据上执行 `local_epochs` 个 epoch 的 SGD，最小化本地交叉熵经验风险，得到上传模型 \(w_{t}^{k}\)。服务端维护一组可训练标量 \(z\in\mathbb{R}^K\)（代码中的 `weight_logits`），经 \(\mathrm{softmax}\) 得到单位单纯形上的向量 \(u\)，再经系数 \(\lambda\in[0,1)\)（`lam`）与均匀向量 \(\frac{1}{K}\mathbf{1}\) 的凸组合并归一化，得到本轮用于写回的全局凸组合权重 \(\tilde{u}\)。另设收缩系数 \(s\in(0,1]\)（`shrink_sum`），客户端侧实际系数为 \(\beta_k=s\,\tilde{u}_k\)，满足 \(\sum_k\beta_k=s\)；剩余质量 \(1-s\) 以本轮聚合前的全局参数 \(w_t\) 作为锚点参与逐层线性混合。

**表 3-1 主要符号**

| 符号 | 含义 |
|------|------|
| \(K\) | 客户端数量 |
| \(t\) | 通信轮次索引 |
| \(\mathcal{D}_k\) | 客户端 \(k\) 的本地训练数据（由索引 \(\mathcal{I}_k\) 诱导） |
| \(\mathcal{D}_p\) | 服务端代理集 |
| \(w_t\) | 第 \(t\) 轮广播前的全局参数 |
| \(w_{t}^{k}\) | 第 \(t\) 轮客户端 \(k\) 本地训练结束后的模型参数 |
| \(\ell(\cdot;\cdot)\) | 多类交叉熵损失（实现为 `nn.CrossEntropyLoss`） |
| \(\eta\) | 客户端本地 SGD 学习率 |
| \(z,u,\tilde{u},\beta\) | 聚合权重的 logit、softmax 输出、\(\lambda\) 混合再归一化后的权重、含 \(s\) 的实际客户端系数 |

#### 3.1.2 联邦学习目标（与 FedAvg 权重对照）

在不移动原始数据的前提下，经典目标可写为各客户端样本量加权的全局经验风险最小化：

\[
\min_{w}\ F(w)=\sum_{k=1}^{K}\frac{n_k}{n}\,L_k(w),\qquad 
L_k(w)=\frac{1}{n_k}\sum_{(x,y)\in\mathcal{D}_k}\ell\big(f(x;w),y\big),
\]

其中 \(n_k=|\mathcal{D}_k|\)，\(n=\sum_k n_k\)，\(f(\cdot;w)\) 为深度网络预测头输出。FedAvg 在聚合阶段取 \(\alpha_k=n_k/n\) 的固定凸组合，与本地目标在 IID 近似下对齐；在统计异质性较强时，该静态权重与真实「全局可泛化解」之间会出现偏差。

#### 3.1.3 本章聚合阶段的形式化（与代码一致的一轮写法）

第 \(t\) 轮在收到 \(\{w_{t}^{k}\}_{k=1}^K\) 后，服务端构造混合参数字典：对每个可学习参数张量，先按 \(\beta\) 在客户端张量栈上做加权求和，再对每个坐标加上 \((1-s)\) 倍的 \(w_t\) 中对应张量，得到「候选全局参数」\(\tilde{w}(u)\)（\(u\) 由当前 \(z\) 经 softmax 得到）。在代理集 mini-batch 上计算平均分类损失 \(\mathcal{L}_p(z)=\mathbb{E}_{(x,y)\sim\mathcal{D}_p}[\ell(f(x;\tilde{w}(u)),y)]\)（实现中对 `proxy_loader` 各 batch 求和再除以 batch 数），对 \(z\) 做一次 SGD（含动量）。步后重新计算 softmax，执行

\[
\tilde{u} \propto (1-\lambda)\,u+\frac{\lambda}{K}\mathbf{1},
\]

归一化后令 \(\beta_k=s\,\tilde{u}_k\)，按上式写回 `global_model` 的 `state_dict`。\(s=1\) 时退化为纯客户端凸组合；\(s<1\) 时显式保留 \((1-s)\) 的全局锚点项。第 \(t+1\) 轮广播的模型即为该写回结果。

上述服务端步骤不要求客户端知晓 \(z\) 或 \(\mathcal{D}_p\)；威胁模型上默认服务端可信且代理集为任务相关的小规模子集，隐私讨论留待后文。

#### 参考文献线索（润色时可保留或改为正式著录）

1. **McMahan 等，FedAvg（AISTATS 2017）**  
   - PDF：<https://arxiv.org/pdf/1602.05629.pdf>  
   - Abs：<https://arxiv.org/abs/1602.05629>  
   - 若绪论/第 1 章已引用，参考文献表可避免重复。

2. **（可选）Sun 等，FedAPA（IJCAI 2025；工程注释对齐的参考实现）**  
   - IJCAI：<https://www.ijcai.org/proceedings/2025/692>  
   - arXiv：<https://arxiv.org/abs/2502.07456>  

---

## English Version

### 3.1 Problem Formalization

#### 3.1.1 Sets and notation

Consider federated image classification with \(K\) clients. At the beginning of communication round \(t\) (\(t=1,\ldots,T\)), the server holds global parameters \(w_t\). Client \(k\) owns a private index set \(\mathcal{I}_k\) that induces a local dataset \(\mathcal{D}_k=\{(x,y)\}\). Raw examples are not exchanged across clients. A small server-side proxy set \(\mathcal{D}_p\) is carved out of the full training set with ratio `proxy_ratio` via a seeded random split in `prepare_data`; it supplies supervised labels on the server and is never shipped to clients.

Client \(k\) runs SGD for `local_epochs` epochs on \(\mathcal{D}_k\) with learning rate \(\eta\) and momentum matching the implementation, minimizing the local cross-entropy risk and producing an upload \(w_{t}^{k}\). The server stores trainable logits \(z\in\mathbb{R}^K\) (`weight_logits`), maps them to \(u=\mathrm{softmax}(z)\), mixes \(u\) with the uniform vector \(\frac{1}{K}\mathbf{1}\) using \(\lambda\in[0,1)\) (`lam`), renormalizes to \(\tilde{u}\), and forms client coefficients \(\beta_k=s\,\tilde{u}_k\) with \(s\in(0,1]\) (`shrink_sum`) so \(\sum_k\beta_k=s\). The remaining mass \(1-s\) blends in the pre-aggregation global tensors \(w_t\) coordinatewise.

**Table 3-1 Main symbols**

| Symbol | Meaning |
|--------|---------|
| \(K\) | Number of clients |
| \(t\) | Communication-round index |
| \(\mathcal{D}_k\) | Local training data of client \(k\) |
| \(\mathcal{D}_p\) | Server-side proxy set |
| \(w_t\) | Global parameters before round-\(t\) aggregation |
| \(w_{t}^{k}\) | Client-\(k\) parameters after local training in round \(t\) |
| \(\ell(\cdot;\cdot)\) | Multiclass cross-entropy (`nn.CrossEntropyLoss`) |
| \(\eta\) | Client-side SGD learning rate |
| \(z,u,\tilde{u},\beta\) | Aggregation logits, softmax weights, \(\lambda\)-mixed renormed weights, scaled client coefficients |

#### 3.1.2 Federated objective (FedAvg baseline)

The classical formulation minimizes a sample-weighted sum of client empirical risks without centralizing private data:

\[
\min_{w}\ F(w)=\sum_{k=1}^{K}\frac{n_k}{n}\,L_k(w),\qquad 
L_k(w)=\frac{1}{n_k}\sum_{(x,y)\in\mathcal{D}_k}\ell\big(f(x;w),y\big),
\]

with \(n_k=|\mathcal{D}_k|\), \(n=\sum_k n_k\), and network outputs \(f(x;w)\). FedAvg fixes \(\alpha_k=n_k/n\) at aggregation time; under strong statistical heterogeneity, that static choice can misalign with a globally useful solution.

#### 3.1.3 Aggregation-stage model used in this chapter (one round, code-aligned)

Given uploads \(\{w_{t}^{k}\}\), the server builds a mixed parameter dict: for each learnable tensor, it forms a client stack weighted by \(\beta\) and adds \((1-s)\) times the matching tensor from \(w_t\), yielding a candidate global map \(\tilde{w}(u)\) induced by the current softmax \(u\) of \(z\). It evaluates the average proxy loss \(\mathcal{L}_p(z)=\mathbb{E}_{(x,y)\sim\mathcal{D}_p}[\ell(f(x;\tilde{w}(u)),y)]\) over the proxy loader, backpropagates into \(z\), and applies one SGD step with momentum. After the step it recomputes softmax, sets

\[
\tilde{u} \propto (1-\lambda)\,u+\frac{\lambda}{K}\mathbf{1},
\]

renormalizes, defines \(\beta_k=s\,\tilde{u}_k\), and writes the mixed tensors into `global_model.state_dict`. The case \(s=1\) removes the explicit global anchor; \(s<1\) keeps a \((1-s)\) mass on the outgoing global tensors. Clients do not observe \(z\) or \(\mathcal{D}_p\). The server is treated as honest for formalization; proxy-data privacy trade-offs belong to later discussion.

#### Reference hints (for your editor / Gemini)

1. **McMahan et al., FedAvg, AISTATS 2017** — PDF: <https://arxiv.org/pdf/1602.05629.pdf>; abs: <https://arxiv.org/abs/1602.05629>. Omit duplicate bibliography entry if already cited in Chapter 1.

2. **Optional: Sun et al., FedAPA, IJCAI 2025** — proceedings: <https://www.ijcai.org/proceedings/2025/692>; arXiv: <https://arxiv.org/abs/2502.07456>.

---

## 给 Gemini 的简要指令（可复制）

请在不改变数学含义与符号定义的前提下，优化中英文表述：统一术语（联邦学习、代理集、聚合权重、收缩系数等），使中文符合学位论文习惯、英文符合学术写作习惯；可微调段落顺序，但不要删减与实现绑定的关键句（`weight_logits`、`proxy_ratio`、`shrink_sum`、`lam`、`functional_call` 与写回 `state_dict` 的对应关系）。
