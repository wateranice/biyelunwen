# 第 3 章 3.4 权重平滑及收缩控制

前两节给出了以代理元损失驱动 \(\boldsymbol{z}\)，经 Softmax 得到 \(\boldsymbol{u}=\mathrm{softmax}(\boldsymbol{z})\) 的聚合权生成逻辑。纯 Softmax 在有限代理样本与高度 Non-IID 场景下，容易使 \(\boldsymbol{u}\) 在少数轮次内**塌缩到近似 one-hot**：某一客户端获得接近 1 的权，其余接近 0——可称之为权空间上的**「独裁」**（Dictatorship）。这既可能对应于该客户端在代理集上偶然占优，也可能放大统计异质性带来的**过拟合式**聚合偏置。为此，服务端在梯度更新之后引入 **\(\lambda\) 均匀混合**；在写回全局参数时再引入 **\(s\)（`shrink_sum`）全局锚点收缩**。二者共同构成方法论的**稳健化层**，本节从机制与理论类比两方面加以升华；数学记号与 `src/AdaptiveWeighted/server_aw.py` 中 `lam`、`shrink_sum` 及写回 `state_dict` 的逻辑一致。

---

## 3.4.1 \(\lambda\) 混合：以均匀先验缓解权重的过快塌缩

设元梯度步后、Softmax 输出为 \(\boldsymbol{u}=(u_1,\ldots,u_K)^\top\in\Delta^{K-1}\)。引入 \(\lambda\in[0,1)\)（实现中为 `lam`），构造**凸组合**  
\[
\boldsymbol{u}'=(1-\lambda)\,\boldsymbol{u}+\lambda\,\boldsymbol{\pi}_0,\qquad 
\pi_{0,k}=\frac{1}{K}\ \ (k=1,\ldots,K),
\]  
即向**离散均匀分布** \(\boldsymbol{\pi}_0\) 插值。由于 \(\sum_k u_k=1\) 且 \(\sum_k \pi_{0,k}=1\)，有 \(\sum_k u'_k=(1-\lambda)+\lambda=1\)，故 \(\boldsymbol{u}'\) 已在单纯形上；实现中仍做一次归一化以抑制浮点误差。记最终用于写回前「客户端凸组合部分」的归一化权为 \(\tilde{\boldsymbol{w}}=(\tilde{w}_1,\ldots,\tilde{w}_K)^\top\)，在理想算术下  
\[
\tilde{w}_k=(1-\lambda)\,u_k+\frac{\lambda}{K}.
\]

**机制解释。** \(\boldsymbol{u}\) 完全由代理监督下的元梯度塑造；当代理集信息量有限或类别覆盖偏斜时，\(\boldsymbol{u}\) 可能在少数方向上增长过快，形成**硬 winner-take-all**。加入 \(\lambda\boldsymbol{\pi}_0\) 等价于在权向量上施加一个**不可被优化掉的均匀底座**：每个客户端至少保留 \(\lambda/K\) 的「底噪份额」，从而  
- 阻止任意 \(u_k\to 1\) 而其余 \(\to 0\) 的极端分配在一步之内被放大到数值病态；  
- 在解释上对应于引入**对称 Dirichlet 先验的极限形态**（浓度趋于 0 的均匀支撑），使聚合在「数据驱动」与「民主先验」之间折中。  
\(\lambda\) 越大，权分布越接近均匀，聚合越接近 FedAvg 的等权思想；\(\lambda\to 0\) 时退化为纯 \(\boldsymbol{u}\)。因此 \(\lambda\) 控制的是**权空间探索—利用**折中：小 \(\lambda\) 更相信代理元损失；大 \(\lambda\) 更抑制由元损失 alone 导致的独裁式塌缩。

---

## 3.4.2 \(s<1\) 时的全局锚点收缩与综合系数 \(\beta_k\)

记本轮聚合**之前**的全局参数为 \(\boldsymbol{\theta}^{t}\)（实现中为当前 `global_model` 在写回前的 `state_dict`），各客户端上传为 \(\boldsymbol{\theta}^{t}_1,\ldots,\boldsymbol{\theta}^{t}_K\)。引入 **\(s\in(0,1]\)**（`shrink_sum`）。在 \(\lambda\) 混合得到 \(\tilde{\boldsymbol{w}}\) 之后，定义**实际参与客户端凸组合的系数**  
\[
\beta_k \;=\; s\,\tilde{w}_k,\qquad k=1,\ldots,K.
\]  
于是  
\[
\sum_{k=1}^{K}\beta_k \;=\; s\sum_{k=1}^{K}\tilde{w}_k \;=\; s.
\]  
剩余质量 \(1-s\) 分配给**旧全局模型**，得到第 \(t+1\) 轮全局参数（逐张量写法）：  
\[
\boxed{
\boldsymbol{\theta}^{t+1}_m
\;=\;
\sum_{k=1}^{K} \beta_k\,\boldsymbol{\theta}^{t}_{k,m}
\;+\;
(1-s)\,\boldsymbol{\theta}^{t}_{m}
}
\]  
其中 \(m\) 遍历所有可学习参数张量。当 \(s=1\) 时，\(\sum_k\beta_k=1\)，退化为「仅客户端凸组合」（在已做 \(\lambda\) 混合的前提下，\(\tilde{\boldsymbol{w}}\) 仍为单纯形向量）；当 **\(s<1\)** 时，全局更新显式包含 **\((1-s)\boldsymbol{\theta}^{t}\)** 项，即**全局锚点收缩**：新全局点并非完全落在客户端上传点的凸包内，而是向本轮起点 \(\boldsymbol{\theta}^{t}\) **拉回**。

**与 \(\lambda\) 的分工（概念上）。** \(\lambda\) 在**权向量**上抑制 Softmax 的极端性；\(s\) 在**参数流形**上抑制相对上一轮全局的跳跃幅度。二者作用于不同对象，可独立调节。

---

## 3.4.3 理论类比：正则化、漂移与 Non-IID 发散

### （1）\(\lambda\) 与权空间的熵正则意象

若把 \(\tilde{\boldsymbol{w}}\) 看成离散概率向量，则 \((1-\lambda)\boldsymbol{u}+\lambda\boldsymbol{\pi}_0\) 可理解为在最大似然（由代理损失诱导的 \(\boldsymbol{u}\)）与**最大熵**倾向（均匀 \(\boldsymbol{\pi}_0\)）之间的插值。熵 \(H(\boldsymbol{w})=-\sum_k w_k\log w_k\) 在 \(\boldsymbol{\pi}_0\) 处最大；向 \(\boldsymbol{\pi}_0\) 拉近会提高权分布熵，从而**惩罚**过于尖锐的聚合分配。这与分类任务中**标签平滑**（Label Smoothing）在目标端引入均匀噪声具有同构之处，只不过此处平滑发生在**聚合权**而非标签分布上。

### （2）\(s<1\) 与参数更新上的「衰减型」正则

记仅由客户端上传给出的**凸组合提案**为  
\[
\hat{\boldsymbol{\theta}}^{t+1}\equiv\sum_{k=1}^{K}\tilde{w}_k\,\boldsymbol{\theta}^{t}_k,
\qquad \sum_k\tilde{w}_k=1.
\]  
则写回式等价于  
\[
\boldsymbol{\theta}^{t+1}
=
s\,\hat{\boldsymbol{\theta}}^{t+1}
\;+\;
(1-s)\,\boldsymbol{\theta}^{t}.
\]  
即：新全局点是「提案 \(\hat{\boldsymbol{\theta}}^{t+1}\)」与「锚点 \(\boldsymbol{\theta}^{t}\)」的凸组合。\(s<1\) 时，更新**不会**完全落到客户端凸包内的某一点，而是被**拉回**向本轮通信开始时的全局参数——这与优化中的 **proximal / trust-region** 意象一致：每一步只在信赖域内部分接纳异质客户端的合力。与经典 **Weight Decay** 惩罚 \(\|\boldsymbol{\theta}\|\) 不同，\((1-s)\boldsymbol{\theta}^{t}\) 惩罚的是**沿联邦时间轴的跳跃幅度**；在 Non-IID 引发的客户端漂移场景下，它抑制全局轨迹被单轮极端上传「拽飞」，起到**轨迹阻尼**作用。

### （3）Non-IID 下的模型发散与两项机制的互补

在 Non-IID 下，各 \(\boldsymbol{\theta}^{t}_k\) 分别朝各自本地经验风险的山谷移动，其凸组合未必仍位于全局可泛化区域；代理元损失虽能在权空间上纠偏，但在小代理集时仍可能过拟合。**\(\lambda\)** 通过均匀底座限制权向量极端化，降低「错误放大某一漂移客户端」的概率；**\(s<1\)** 通过锚定 \(\boldsymbol{\theta}^{t}\) 限制参数轨迹的**瞬时曲率**，使全局模型不会完全追随某一轮异质上传的合力而「飞出」稳定盆地。二者与 3.3 节的**梯度驱动**形成分工：3.3 负责「朝代理可解释方向学习 \(\boldsymbol{u}\)」；3.4 负责「学习到的 \(\boldsymbol{u}\) 在写入全局时仍保持统计与几何上的谦逊」。这正是本节作为**方法理论升华**的定位。

---

## 3.4.4 小结：综合聚合公式链

将两阶段写在一起，本轮（省略张量下标 \(m\)）的最终全局更新为  
\[
\boldsymbol{\theta}^{t+1}
=
(1-s)\,\boldsymbol{\theta}^{t}
\;+\;
s\sum_{k=1}^{K}\tilde{w}_k\,\boldsymbol{\theta}^{t}_k,
\qquad
\tilde{w}_k
=
\frac{(1-\lambda)\,u_k+\lambda/K}
{\sum_{j=1}^{K}\big((1-\lambda)\,u_j+\lambda/K\big)},
\quad
\beta_k=s\,\tilde{w}_k.
\]  
（分母在精确算术下为 1。）**\(\beta_k\)** 为实现中返回的、用于日志与可视化的**客户端实际系数**（\(\sum_k\beta_k=s\)）；其与 \(\tilde{w}_k\)、\(s\)、\(\lambda\)、\(\boldsymbol{u}\) 的关系由上式完全确定。至此，从 Softmax 元梯度到 \(\lambda\) 平滑再到 \(s\) 锚点收缩，形成**可解释、可调参、可写进任务书**的完整聚合控制链。

---

*（实现对应：`server_aw.py` 中 `w_final = (1-lam)*w + lam/K` 归一化、`beta = shrink_sum * w_final`、以及 `new_sd[name] = client_part + residual * global_sd[name]`。）*
