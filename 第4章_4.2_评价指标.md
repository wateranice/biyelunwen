# 4.2 评价指标

为定量比较不同联邦聚合策略的有效性，本节从 **全局分类性能**、**通信效率** 与 **服务器端代理学习** 三个角度定义评价指标。实验均在同一测试集上计算 **Top-1 分类准确率**，通信以「一轮全局聚合 + 各客户端本地更新」为 **1 个通信轮次**。

## （1）分类准确率（Accuracy）

设测试集共有 \(N\) 个样本，第 \(t\) 轮通信后全局模型为 \(f^{(t)}\)。对样本 \(i\)，真实标签为 \(y_i\)，模型预测为 \(\hat{y}_i^{(t)} = \arg\max_c f^{(t)}(x_i)_c\)。则 **Top-1 准确率**定义为正确预测样本所占比例：

\[
\mathrm{Acc}^{(t)} = \frac{1}{N}\sum_{i=1}^{N} \mathbb{1}\bigl[\hat{y}_i^{(t)} = y_i\bigr] \times 100\%\,.
\]

该指标反映当前全局模型在 **IID 测试集** 上的整体判别能力，数值越高表示联邦训练效果越好。实验中记录每一通信轮次结束后的 \(\mathrm{Acc}^{(t)}\)，用于绘制准确率—轮次曲线及横向对比 FedAvg、FedProx 与本文方法。

## （2）达到目标精度所需的通信轮次（Communication Rounds）

为衡量 **收敛速度** 与 **通信代价**，引入一组事先给定的 **目标准确率阈值** \(T\)（单位：\(\%\)）。定义 **首次达到阈值** 的通信轮次为：

\[
R(T) = \min\bigl\{ t \in \{1,2,\ldots,T_{\max}\} \;\big|\; \mathrm{Acc}^{(t)} \ge T \bigr\}\,,
\]

其中 \(T_{\max}\) 为预设的最大通信轮数。若在全部轮次内准确率始终未达到 \(T\)，则记 \(R(T)\) 为 **未达到**（实现中对应汇总表的空缺或占位，便于与「更快达到同一精度」的方法比较）。

**与实验脚本一致：** 本文第 4 章对比实验采用 **三个** 目标阈值，用于统计不同难度下的通信效率：

\[
T \in \{50\%,\; 55\%,\; 60\%\}\,.
\]

即：分别记录全局测试准确率 **首次不低于 50%、55%、60%** 时对应的通信轮次 \(R(50)\)、\(R(55)\)、\(R(60)\)。**最大训练轮数** 在配置中取 \(T_{\max}=30\)（与 `configs/ch4_compare.yaml` 中 `global_rounds` 一致），表示 **预算轮次上限**，不是「目标精度」本身。

## （3）代理集损失（Proxy Loss）及其变化

在 **可学习聚合权重** 设定下，服务器在每轮聚合时持有一组与客户端一一对应的权重（经 Softmax 归一化），并在 **代理数据集** \(\mathcal{D}_p\) 上对 **混合参数** 下的交叉熵进行优化。设第 \(t\) 轮代理集划分为 \(B_t\) 个 mini-batch，混合后的可微参数记为 \(\theta^{(t)}_{\mathrm{mix}}(\boldsymbol{\beta})\)（由当前客户端模型与聚合系数 \(\boldsymbol{\beta}\) 共同决定，实现上通过 `functional_call` 在全局网络结构上替换参数）。则 **代理集经验损失** 可取为各 batch 交叉熵的 **batch 平均**：

\[
\mathcal{L}_{\mathrm{proxy}}^{(t)}
= \frac{1}{|B_t|}\sum_{(x,y)\in B_t}
\mathrm{CE}\bigl(f(\cdot;\theta^{(t)}_{\mathrm{mix}})(x),\, y\bigr)\,,
\]

其中 \(\mathrm{CE}\) 为交叉熵损失。该标量刻画了 **「当前聚合系数 + 客户端更新」在代理集上的拟合程度**；随通信轮次 \(t\) 变化绘制 \(\mathcal{L}_{\mathrm{proxy}}^{(t)}\) 曲线，可观察服务器端权重学习是否稳定、是否与测试准确率提升相一致。

---

## 与实现、配置的对应关系（备忘）

| 概念 | 代码/配置位置 |
|------|----------------|
| 目标准确率阈值 \(T\in\{50,55,60\}\%\) | `experiments/comparison_aligned.py` 参数 `--thresholds 50,55,60` |
| 最大通信轮数 \(T_{\max}=30\) | `configs/ch4_compare.yaml` 中 `server.global_rounds` |
| 代理集上的 batch 平均交叉熵 | `src/AdaptiveWeighted/server_aw.py` 中 `ServerLearnableAggregator.aggregate_round`：对 `proxy_loader` 各 batch 累加 `CrossEntropyLoss` 后除以 batch 数 |
| 测试集 Loss–通信轮次（任务书收敛曲线） | `fl_system.py`：`test_loss` 写入 `*_run_log.npz`；`comparison_aligned.py`：`test_loss_*` 写入 `comparison_*.npz`，并输出 `comparison_loss_beta*.png` |
| 代理集损失 \(\mathcal{L}_{\mathrm{proxy}}\)（可学习路径 meta-loss） | `server_aw.py`：`proxy_loss_meta` 经 `adaptive_aggregate` 第三返回值；`fl_system.py`：`proxy_loss` 写入 `*_run_log.npz`；`comparison_aligned.py`：`proxy_loss_adaptive`（FedAvg/FedProx 行为 `nan`）+ `comparison_proxy_loss_beta*.png`；`plot_run_log.py`：`*_proxy_loss_from_log.png` |

**说明：** 「目标精度」指上述 **50%、55%、60%** 三档阈值；**30 轮** 为轮次预算上限，二者含义不同。
